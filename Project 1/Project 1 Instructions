1.	Write a program that calls fork(). Before calling fork(), have the main process access a variable (e.g., x) and set its value to 100.
 
2.	Write a program that calls fork() and then calls some form of exec() to run the program /bin/ls.

3.	Write a program that uses wait() to wait for the child process to finish in the parent. 
